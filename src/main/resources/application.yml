cors:
  origins:
    patterns: http://localhost:8080,http://localhost:3000,https://www.example.com
file:
  upload-dir: /home/bverly/Downloads/downloads_projeto_spring
security:
  jwt:
    token:
        secret-key: 53cr3t7
        expire-length: 3600000 # 1 hora em milliseconds
email:
  subject: "Default Subject"
  message: "Default Message"
spring:
  application:
    name: projeto_rest_spring_boot
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://azuremysqlbruno.mysql.database.azure.com/projeto_rest_spring_boot?useTimezone=true&serverTimezone=UTC
    username: master
    password: bJb%2up0vHcSE^
  jackson:
    date-format: yyyy-MM-dd
    serialization:
      write-dates-as-timestamps: false
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false
    open-in-view: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME}
    password: ${EMAIL_PASSWORD}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.starttls.required: true
      mail.debug: true
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 200MB
      max-request-size: 200MB
logging:
  level:
    root: INFO


  spring-doc:
    paths-to-match: /api/**/v1/**
    swagger-ui:
      use-root-path: true



